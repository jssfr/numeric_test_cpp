add_library(math)
add_library(MATH::MATH ALIAS math)
target_sources(math
    PUBLIC
    FILE_SET all_my_modules TYPE CXX_MODULES FILES
    math.ixx
    math-ncrnpr.ixx
    math-numerical.ixx
    math-compose.ixx
    math-variable.ixx
    math-functions.ixx
    math-calculus.ixx
)

add_compile_definitions(CONTAINERS
)

target_link_libraries(math CONFIG::CONFIG)
target_include_directories(math PUBLIC ${IMPORT_DIR}/headers)
